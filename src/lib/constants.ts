// Shift configuration
/** Expected shift duration in hours (Requirement 6.1) */
export const SHIFT_DURATION_HOURS = 8;

/** Expected start hour (0-23) for on-time arrival calculation (Requirement 3.2) */
export const EXPECTED_START_HOUR = 10;

// Leave allowances per year
/** Annual leave days allowance (Requirement 5.1) */
export const ANNUAL_LEAVE_ALLOWANCE = 18;

/** Annual WFH days allowance (Requirement 5.1) */
export const ANNUAL_WFH_ALLOWANCE = 8;

// Pagination
/** Default page size for list views (Requirement 15.1) */
export const DEFAULT_PAGE_SIZE = 20;
